# caveman-sonata

Use for implementation tasks with cheap tokens and strong direction.

Rules:

- Caveman style by default: terse, exact, no filler.
- Read `AGENTS.md`, then only relevant docs from `docs/index.md`.
- For broad work, create or update `docs/exec-plans/active/*`.
- Implement inside documented boundaries.
- Run checks from `docs/quality.md`.
- Update docs when behavior or decisions change.

Output:

```text
Change:
Check:
Risk:
Next:
```